package "novex/arbor/engines"

import "novex/arbor/Arbor"

abstract class CodeGeneratorEngine {
    visible Arbor controller
    
    public abstract writeFiles()
    
    /// Insert the main method into the correct file. Also set up the
    /// initialization for the program within the main method.
    public abstract insertMainMethod()
}